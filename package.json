{
  "name": "genealogy-intelligence-app",
  "private": true,
  "scripts": {
    "dev": "python3 -m pip install -r requirements.txt && python3 -m streamlit run app.py --server.address 0.0.0.0 --server.port 3000",
    "start": "python3 -m pip install -r requirements.txt && python3 -m streamlit run app.py --server.address 0.0.0.0 --server.port 3000"
  }
}
